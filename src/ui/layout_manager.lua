-- Ce fichier sera supprimé dans la prochaine phase 
-- de la refactorisation suite à l'adoption de l'architecture unifiée.
-- Pour l'instant, il contient ce commentaire explicatif pour éviter d'être un fichier vide,
-- mais son contenu sera définitivement supprimé lors de la mise à jour du UIManager.

-- Les fonctionnalités de layout sont désormais intégrées dans:
-- 1. ComponentBase, qui gère le positionnement et le dimensionnement de base
-- 2. UIManager, qui gère l'organisation globale des composants
-- 3. Chaque composant individuel, qui gère son organisation interne

-- Ne pas ajouter de code à ce fichier, car il est marqué pour suppression.